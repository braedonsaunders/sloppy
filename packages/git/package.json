{
  "name": "@sloppy/git",
  "version": "1.0.0",
  "description": "Git operations handler for Sloppy code quality tool",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest",
    "test:coverage": "vitest --coverage",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "simple-git": "^3.22.0",
    "@sloppy/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vitest": "^1.2.0"
  },
  "files": [
    "dist"
  ],
  "license": "MIT"
}
